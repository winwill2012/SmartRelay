---
description: 会话内完成可上线改动后自动 git 推送，并对生产机部署后端与 admin 前端（含 scp 兜底）
alwaysApply: true
---

# 生产环境自动部署（后端 + 管理后台前端）

**不必等用户再说「部署」**：凡本会话中**已完成**且需作用到线上的改动（含 **`backend/`**、**`admin-web/`**、部署相关脚本或 `deploy/` 配置等），**默认**按下面流程 **git 提交并推送**，并 **部署到生产机**。仅当用户明确说 **不要部署 / 仅本地 / 稍后再说** 时跳过。

## 生产环境约定

- **主机**：`root@119.29.197.186`（本机已配置免密 SSH 时使用 `BatchMode` 非交互执行）。
- **代码目录**：`/opt/smart-relay`。
- **后端服务**：`smartrelay-api`（`WorkingDirectory=/opt/smart-relay/backend`）。
- **管理后台静态资源**：Nginx 提供 ` /smart-relay/admin/` → **`/opt/smart-relay/admin-web/dist/`**（见 `deploy/iot.welinklab.com.conf`）。

## 1. 本机 Git（始终先做）

在仓库根目录 `G:/SmartRelay`：

1. `git add` 本次相关改动，`git commit -m "简短中文"`。
2. `git push origin master`（或当前跟踪分支）。

若 **`git push` 失败**：说明原因、保留本地提交；仍可仅用 **下文 scp 兜底** 把当前工作区同步到服务器（不依赖远程已更新）。

## 2. 生产机更新方式

生产机若 **`git fetch` 因 HTTPS 无凭据失败**，**不要**强依赖服务器拉 GitHub；改用 **本机打包容器 + scp + 服务器解压**（与历史成功部署一致）：

1. **本机构建前端**：`npm run build --prefix admin-web`。
2. **打包**（在仓库根目录，排除后端虚拟环境；PowerShell / CMD 可用）：

```bash
tar.exe --exclude=backend/.venv --exclude=__pycache__ -czf _deploy_smartrelay.tgz backend admin-web/dist
```

3. **上传并部署**（一条命令内解压、`pip`、`restart`，健康检查略等几秒再 `curl`）：

```bash
scp -o BatchMode=yes _deploy_smartrelay.tgz root@119.29.197.186:/tmp/smartrelay-deploy.tgz
ssh -o BatchMode=yes root@119.29.197.186 "set -e; cd /opt/smart-relay && tar -xzf /tmp/smartrelay-deploy.tgz && rm -f /tmp/smartrelay-deploy.tgz && cd backend && ./.venv/bin/pip install -r requirements.txt -q && systemctl restart smartrelay-api && sleep 4 && curl -sS http://127.0.0.1:8000/health"
```

4. **清理本机**临时包：`del _deploy_smartrelay.tgz`（Windows）或 `rm -f _deploy_smartrelay.tgz`。

**若服务器已改为 SSH remote 且 `git fetch` 可用**，可改用：`cd /opt/smart-relay && git fetch && git reset --hard origin/master`，再只对后端执行 `pip` + `restart`；**前端仍需**本机 `npm run build` 后将 **`admin-web/dist`** 同步到服务器（或仍用上述 tarball 一次带上 `admin-web/dist`）。

## 与数据库迁移的关系

- 若本次变更包含 **库表结构**：先遵守 `database-migrations.mdc` 执行迁移，再部署并重启后端。

## 注意

- 不要将密钥、密码写入规则正文。
- 解压会覆盖 `/opt/smart-relay` 下与压缩包同路径文件；服务器仅有本地、未入库的修改可能被覆盖。
