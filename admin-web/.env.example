# 后端 HTTP API Base（须包含 /api/v1，与《协议标准.md》一致）
# 生产构建（npm run build）：见 .env.production
#
# 本地 npm run dev：
# - 默认走 Vite 代理 /api →见 .env.development 中 VITE_DEV_API_PROXY（可指向线上，无需本机 8000）
# - 若本机跑 uvicorn：在 .env.development.local 写 VITE_DEV_API_PROXY= 空值，或删掉 VITE_DEV_API_PROXY 行
# 若需浏览器直连后端（不走代理）：
# VITE_API_BASE=http://127.0.0.1:8000/api/v1
