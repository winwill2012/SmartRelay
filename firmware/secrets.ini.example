; 可选：复制为 secrets.ini，并在 platformio.ini 的 [platformio] 中取消注释：
;   extra_configs = secrets.ini
; 然后在下方用 build_flags 覆盖宏（需自行处理 Windows 下引号转义）。
; 更简单的方式：直接本地修改 src/config.h 中的 CFG_MQTT_PASS，勿提交密码。

[env:esp32-c3-devkitm-1]
build_flags =
    ${env:esp32-c3-devkitm-1.build_flags}
