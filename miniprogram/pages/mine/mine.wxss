.page {
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
}

.card-gradient {
  margin: 32rpx 32rpx 0;
  padding: 40rpx 32rpx 32rpx;
  border-radius: 32rpx;
  background: linear-gradient(145deg, #f0f9ff 0%, #e0f2fe 40%, #eef2ff 100%);
  border: 2rpx solid rgba(191, 219, 254, 0.8);
  box-shadow: 0 8rpx 24rpx rgba(37, 99, 235, 0.08);
}

.guest {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.avatar-btn {
  width: 128rpx;
  height: 128rpx;
  border-radius: 9999rpx;
  background: #2563eb;
  color: #fff;
  font-size: 28rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(37, 99, 235, 0.3);
}

.guest-tip {
  margin-top: 24rpx;
  font-size: 24rpx;
  color: #64748b;
}

.user-top {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.avatar-static {
  width: 128rpx;
  height: 128rpx;
  border-radius: 9999rpx;
  background: linear-gradient(145deg, #eff6ff 0%, #dbeafe 100%);
  border: 4rpx solid #dbeafe;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 12rpx rgba(37, 99, 235, 0.15);
}

.avatar-static-icon {
  font-size: 56rpx;
  color: #2563eb;
  line-height: 1;
}

.nickname-btn {
  margin-top: 28rpx;
  width: 100%;
  max-width: 420rpx;
  padding: 18rpx 24rpx;
  text-align: center;
  background: rgba(37, 99, 235, 0.08);
  border-radius: 20rpx;
  border: 2rpx solid rgba(147, 197, 253, 0.9);
  box-sizing: border-box;
}

.nickname-text {
  font-size: 34rpx;
  font-weight: 600;
  color: #1e293b;
}

.nickname-text.placeholder {
  color: #94a3b8;
}

.stats {
  margin-top: 36rpx;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20rpx;
}

.stat-box {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 24rpx;
  padding: 18rpx 14rpx;
  border: 2rpx solid #f1f5f9;
  text-align: center;
}

.stat-label {
  font-size: 24rpx;
  color: #64748b;
  font-weight: 500;
}

.stat-num {
  display: block;
  margin-top: 8rpx;
  font-size: 42rpx;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
  color: #0f172a;
}

.stat-num.primary {
  color: #2563eb;
}

.stat-num.muted {
  color: #94a3b8;
}

.menu {
  margin: 32rpx 32rpx 0;
  overflow: hidden;
}

.menu-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 32rpx;
}

.menu-row.border {
  border-bottom: 2rpx solid #f8fafc;
}

.menu-left {
  display: flex;
  align-items: center;
  gap: 24rpx;
  font-size: 30rpx;
  color: #1e293b;
}

.menu-ico {
  width: 72rpx;
  height: 72rpx;
  border-radius: 16rpx;
  background: #fffbeb;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
}

.menu-row:nth-child(2) .menu-ico {
  background: #f5f3ff;
}

.menu-right {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.badge-num {
  min-width: 36rpx;
  padding: 4rpx 12rpx;
  border-radius: 9999rpx;
  font-size: 20rpx;
  text-align: center;
}

.badge-num.on {
  background: #ef4444;
  color: #fff;
}

.badge-num.off {
  background: #e2e8f0;
  color: #64748b;
}

.chev {
  color: #cbd5e1;
  font-size: 32rpx;
}

.logout-wrap {
  margin: 48rpx 32rpx 0;
}

.btn-logout {
  width: 100%;
  border-radius: 24rpx;
  border: 2rpx solid #e2e8f0;
  background: #fff;
  color: #dc2626;
  font-size: 28rpx;
  font-weight: 500;
  line-height: 84rpx;
  padding: 0;
}

.btn-logout::after {
  border: none;
}
